China HSR Simulation Progress Log
=================================

Project overview:
This project implements a realistic China high-speed rail simulation using local station, train-route, and HOTOSM railway data. The most important behavior is segment-aware booking: seats are occupied only over the station interval a passenger travels and become available for later non-overlapping passengers after alighting.

Session log:

2026-05-02T10:41:20Z | branch: root git history unavailable
Feature/bug worked on: Project initialization and simulation design.
Files touched: handoff.md, feature_list.json, agent-progress.txt, README.md, package.json, vite.config.js, .env.example.
Tests run: Initial inventory only; implementation tests pending.
Result: In progress. The root repository reports `fatal: bad object HEAD`, so this folder carries its own explicit harness and verification script.

2026-05-02T11:06:24Z | branch: main
Feature/bug worked on: Complete first implementation slice for all current feature list entries.
Files touched: scripts/prepare-data.cjs, src/algorithms/*, src/simulation_core/*, src/visualization/*, src/styles/app.css, tests/*, README.md, feature_list.json, handoff.md, package-lock.json, public generated data artifacts.
Tests run: `npm run prepare:data` (3058 stations, 7278 HSR records, 280 simulation routes, 2200 rail features); `npm test` (3/3 passing); `npm run build` (passing with Mapbox bundle-size warning only); `rg "sk\\.ey" .` (no matches); `./init.sh` (complete); `curl -I http://127.0.0.1:5174/` (HTTP 200).
Result: Passing. The app is running locally at `http://127.0.0.1:5174/`. The browser default uses only the public Mapbox token; no secret token is stored.

2026-05-02T11:17:49Z | branch: main
Feature/bug worked on: Broaden route coverage and scale train services after user reported route clustering.
Files touched: scripts/prepare-data.cjs, src/simulation_core/SimulationEngine.js, src/App.jsx, src/visualization/BookingPanel.jsx, src/visualization/Dashboard.jsx, tests/dataDiversity.test.mjs, tests/engine.test.mjs, public generated data artifacts, handoff.md, agent-progress.txt.
Tests run: `npm run prepare:data` (3058 stations, 7278 HSR records, 1200 simulation routes, 85 origins, 27 corridors, 2200 rail features); full-engine scale probe (1500 trains, 170 active, 389 visible, 1500 booking options, 27 corridors, 27 origin provinces); `npm test` (5/5 passing); `npm run build` (passing with Mapbox bundle-size warning only); `./init.sh` (complete).
Result: Passing. The map now receives active and near-term services from a diversified nationwide network instead of the first few CSV corridors, and the booking UI can book across the full scheduled train set.

2026-05-02T15:02:00Z | branch: main
Feature/bug worked on: Fix refused local connection and add operational realism.
Files touched: package.json, README.md, scripts/serve-static.cjs, src/simulation_core/SimulationEngine.js, src/visualization/Dashboard.jsx, src/styles/app.css, tests/engine.test.mjs, handoff.md, agent-progress.txt.
Tests run: `npm test` (6/6 passing); `npm run build` (passing with Mapbox bundle-size warning only); `./init.sh` (complete); `curl -I http://127.0.0.1:5174/` (HTTP 200); `launchctl print gui/$(id -u)/com.codex.china-hsr-simulation` (state running, PID 22517); full-engine scale probe (1500 trains, 181 active, 400 visible, 67 no-show releases, 7.8 min average delay, 1124 delayed trains, 16 station hotspots).
Result: Passing. The site is reachable at the in-app browser URL through a launchctl-managed static server, and the simulation now models no-show releases, delay pressure, and station platform congestion.

2026-05-02T17:57:01Z | branch: main
Feature/bug worked on: Implement browser multithreading and continue realism/map refinements.
Files touched: src/simulation_core/simulationWorker.js, src/simulation_core/SimulationWorkerClient.js, src/App.jsx, src/visualization/BookingPanel.jsx, src/visualization/Dashboard.jsx, src/visualization/HSRMap.jsx, src/simulation_core/SimulationEngine.js, src/simulation_core/geo.js, src/styles/app.css, scripts/prepare-data.cjs, tests/dataDiversity.test.mjs, tests/engine.test.mjs, README.md, handoff.md, agent-progress.txt, public generated data artifacts.
Tests run: `npm run prepare:data` (3058 stations, 7278 HSR records, 1200 routes, 85 origins, 27 corridors, 8000 rail features); `npm test` (7/7 passing); `npm run build` (passing, emits `simulationWorker-*.js`); `./init.sh` (complete); `curl -I http://127.0.0.1:5174/` (HTTP 200); full-engine probe (1500 trains, 1200 routes, 1.25 trains/route, 554 seats/train, revenue and passengers increased after 16 ticks, 52.7% rail-matched segments).
Result: Passing. The simulation engine now runs in a browser Web Worker, live demand changes revenue and passengers over time, train movement uses HOTOSM rail-corridor polylines where matched, train markers are smaller, station markers are dots/squares/diamonds, and the dashboard shows quota and trains-per-route.

Next steps:
1. Improve realism by adding an authoritative full timetable source if available.
2. Add a dedicated algorithm comparison page for exact small-instance optimization versus the production heuristic.
3. Add scenario export/replay so runs can be reproduced from a saved seed and demand profile.

2026-05-02T20:19:11Z | branch: main
Feature/bug worked on: Calendar surge, high-volume operations, and smoother train animation.
Files touched: src/simulation_core/SimulationEngine.js, src/simulation_core/simulationWorker.js, src/visualization/HSRMap.jsx, scripts/prepare-data.cjs, .env.example, feature_list.json, handoff.md, public generated data artifacts.
Tests run: `npm run prepare:data` (3058 stations, 7278 HSR records, 1200 routes, 85 origins, 27 corridors, 8000 rail features); `npm test` (8/8 passing); `npm run build` (passing, emits `simulationWorker-DhahEsPn.js`); `./init.sh` (complete); `curl -I http://127.0.0.1:5174/` (HTTP 200); `rg "sk\\.ey" .` (no matches); full-engine probe (6000 trains, 663 active, 2,692,848 passengers, 6.62x prior 407,005 passenger baseline, min/median/max services per route 4/5/7, Jan 1 New Year surge, 1.28x price surge, p95 quote 0.057 ms); in-app browser screenshot and Dashboard DOM verification.
Result: Passing. The simulation now starts on Jan 1 with a 24-hour clock, holiday/weekend/peak-season calendar logic, New Year surge demand/pricing, at least two trains per route and 6000 total trains by default, progressive worker demand preload for browser responsiveness, smaller/interpolated train markers, and dashboard metrics for calendar day, demand surge, price surge, route daily range, and worker cadence. The stable URL remains http://127.0.0.1:5174/.
Next steps:
1. Add an authoritative stop-by-stop timetable source if available to replace simulation-derived intermediate stops.
2. Add a scenario/replay export so calendar, demand, and seed choices can be reproduced from the UI.
3. Add a dedicated algorithm comparison view for exact small-instance optimization versus the production heuristic.

2026-05-03T16:55:00Z | branch: main
Feature/bug worked on: Reliability/robustness/performance overhaul + booking ledger persistence.
Files touched: scripts/prepare-data.cjs (full rewrite of geometry pipeline), scripts/oceanbase_seed.py (added route_geometry/calendar_summary/bookings tables, progress logging), scripts/oceanbase_booking_ingest.py (new), scripts/serve-static.cjs (added /ingest-bookings endpoint), src/simulation_core/SimulationEngine.js (skipped-station processing, ledger, deterministic IDs, dead-code removal, year-end fix), src/simulation_core/simulationWorker.js (ledger flusher), src/App.jsx (ledger endpoint config), tests/dataDiversity.test.mjs (raised thresholds + Xi'an regression), tests/geometryValidation.test.mjs (new), tests/bookingLedger.test.mjs (new), README.md, README.zh-CN.md, NEXTSTEPS.md, handoff.md, public generated data artifacts.
Tests run: `npm run prepare:data` (96.3% known endpoints, 224 origins, 28 corridors, 70.4% rail-traced, 99.4% rail-matched, 0 big jumps); `npm test` (16/16 passing including 4 new geometry/coverage tests + 2 new ledger tests); `npm run build` (passing); `python3 scripts/oceanbase_seed.py --skip-db --days 365` (5,469,688 trains, 2,573,835,026 passengers, ¥915,093,470,567 in 1.87s); curl /ingest-bookings POST verified (NDJSON file written, dispatch flagged when OB_PASSWORD set); full-engine probe (6000 trains, 774 active, 2,741,460 passengers, p95 quote 0.1 ms, 339 Xi'an services bookable).
Result: Passing. Geometry quality jumped from 52.7% rail-matched to 99.4% (70.4% via A* graph traversal); 0 trains-over-water defects; 67 Xi'an routes (was 0); booking ledger persisted to OceanBase via NDJSON HTTP ingest with idempotent upsert and backpressure-tolerant retry. Simulation engine handles fast ticks correctly (no skipped boarding events). All 16 tests deterministic and < 2s. README/handoff/NEXTSTEPS updated.
Next steps:
1. Wire dashboard panel for booking ledger row counts from `/healthz` or a `/ledger-stats` endpoint.
2. Add scenario replay using the persisted booking ledger.
3. Consider per-segment hourly demand fact table for "what's the busiest hour on Beijing→Shanghai during Spring Festival" queries.

2026-05-11T12:30:03Z | branch: main
Feature/bug worked on: Route progression / geometry / OceanBase robustness pass after user reported train oscillation and direct route shortcuts.
Files touched: .gitignore, README.md, README.zh-CN.md, handoff.md, scripts/oceanbase_booking_ingest.py, scripts/oceanbase_seed.py, scripts/serve-static.cjs, src/simulation_core/SimulationEngine.js, src/simulation_core/geo.js, src/simulation_core/simulationWorker.js, src/visualization/HSRMap.jsx, tests/bookingLedger.test.mjs, tests/engine.test.mjs, tests/geometryValidation.test.mjs.
Summary: Added a monotonic route-state guard and serialized routeProgress so trains cannot regress between stations; map interpolation now snaps instead of drawing a long visual chord on route regressions, multi-segment jumps, or large coordinate jumps; polyline length metrics are cached; geometry tests now reject endpoint mismatches, long straight fallbacks, sparse long segments, and >90 km single-hop shortcuts; ledger flushing now requeues on non-2xx HTTP responses; the static server exposes `/ledger-stats`; booking ingest has a credential-free dry-run validator; OceanBase seed connection failures now show an operator message instead of a traceback; generated `logs/` are ignored because prior logs contained secret-looking tokens.
Tests run: `node --test tests/engine.test.mjs tests/geometryValidation.test.mjs tests/bookingLedger.test.mjs` (16/16 focused passing); `npm test` (20/20 passing); `npm run build` (passing); `./init.sh` (data prep, 20 tests, build, secret scan all complete); `python3 -m py_compile scripts/oceanbase_seed.py scripts/oceanbase_booking_ingest.py` (passing); `python3 scripts/oceanbase_seed.py --skip-db --days 7 --workers 2 --chunk-days 2 --run-id codex-skipdb-20260511T1224Z --summary-path /tmp/chinahsr-oceanbase-skipdb-summary.json` (8,400 route-day rows, 110,932 trains, 52,644,235 passengers, ¥22,627,037,838.91 revenue); `curl -I http://127.0.0.1:5174/` (HTTP 200); `curl /healthz` and `curl /ledger-stats` (queue metadata returned).
Result: Passing for code-level robustness and local serving. Real OceanBase table-load smoke was attempted with configured `.env`, but OceanBase Desktop was not listening at `127.0.0.1:2881`; the script now reports that cleanly and recommends starting the tenant or using `--skip-db`.
Next steps:
1. Start OceanBase Desktop / MySQL-mode tenant and rerun a real `oceanbase_seed.py` load to capture table row counts.
2. Consider adding a `/ledger-replay` operator command to replay buffered NDJSON files after OceanBase downtime.
3. If a full authoritative stop-by-stop timetable source becomes available, replace simulated intermediate stops with that source and keep the geometry guardrails.

2026-05-11T13:35:31Z | branch: main
Feature/bug worked on: OceanBase route contracts and terminal turnaround return trips.
Files touched: scripts/prepare-data.cjs, scripts/oceanbase_seed.py, src/simulation_core/SimulationEngine.js, src/visualization/HSRMap.jsx, src/visualization/Dashboard.jsx, tests/engine.test.mjs, tests/geometryValidation.test.mjs, tests/oceanbaseSeed.test.mjs, tests/bookingLedger.test.mjs, README.md, README.zh-CN.md, feature_list.json, handoff.md, public generated data artifacts.
Summary: Added explicit routeContract metadata to every generated route with outbound and return variant IDs, ordered station names, reverse station names, and a stable stop-sequence hash. OceanBase schema/loading now includes route_variants, route_variant_stops, route_variant_segments, route_variant_geometry, and raw HOTOSM rail_tracks so SQL can query every route's exact stop order and geometry in both directions. The engine now treats each train as a two-leg service: outbound route, terminal turnaround dwell, return route through the exact reversed stops/segments, then final completion at the original station. Snapshots expose routeVariantId, direction, legIndex, and journeyProgress; the map snaps on route-variant changes instead of drawing a chord across the turnaround.
Tests run: `npm run prepare:data` (1,200 routes, 7,338 segments, 82.8% rail-traced, 0 straight fallbacks); focused `node --test tests/engine.test.mjs tests/geometryValidation.test.mjs tests/oceanbaseSeed.test.mjs` (16/16 passing); `npm test` (22/22 passing); `npm run build` (passing); `python3 -m py_compile scripts/oceanbase_seed.py` (passing); `python3 scripts/oceanbase_seed.py --skip-db --days 7 --workers 2 --chunk-days 2 --run-id codex-route-contract-20260511T1310Z --summary-path /tmp/chinahsr-route-contract-summary.json` (2,400 variants, 17,076 variant stops, 14,676 variant geometry rows, 12,000 rail tracks in summary); `python3 scripts/oceanbase_seed.py --skip-db --days 365 --workers 4 --chunk-days 8 --run-id codex-route-contract-20260511T1320Z --summary-path public/oceanbase-yearly-summary.json` (438,000 route-day rows, 5,538,508 train services, 2,606,735,581 passengers); `./init.sh` (data prep, 22 tests, production build, secret scan complete); `nc -z 127.0.0.1 2881` (exit 1, OceanBase still not listening).
Result: Passing for code-level route contracts, return-trip behavior, generated data, dry-run OceanBase summary, production build, and secret scan. Live OceanBase table-load smoke remains blocked until OceanBase Desktop / the MySQL-mode tenant is started on `127.0.0.1:2881`.
Next steps:
1. Start OceanBase Desktop / MySQL-mode tenant and rerun `OB_PASSWORD=... npm run oceanbase:seed` to capture live row counts for route_variants, route_variant_stops, route_variant_geometry, and rail_tracks.
2. Add a visual route-audit overlay if further route-following issues are observed, showing the active route variant polyline and the train's snapped position along it.
3. Replace simulation-derived intermediate stops with an authoritative stop-by-stop timetable source if one becomes available.

2026-05-13T10:36:19Z | branch: main
Feature/bug worked on: 12306 SQLite review, OceanBase migration tooling, and Tencent CVM deployment plan.
Files touched: scripts/migrate_12306_to_oceanbase.py, tests/12306Migration.test.mjs, docs/12306-db-review.md, docs/tencent-cvm-oceanbase-runbook.md, package.json, README.md, .gitignore, handoff.md, agent-progress.txt.
Summary: Reviewed `/Users/rogerlin/Downloads/chinashsr/12306.db` as an immutable SQLite input. The database has integrity `ok` and 255,938 reviewed rows: 3,365 stations, 388 train routes, 4,760 ordered route stops, 331 ticket rows, 1,271 price rows, 226,613 rail LineStrings, 3,345 station locations, and 15,865 station-track links. Route quality is strong: every route has stops, no duplicate station orders, no non-1 starts, no stop-order gaps, and all distinct route station names match the station table. Added a migration script that emits OceanBase DDL, review manifests, sample route-query SQL, and can bulk upsert into a reachable OceanBase MySQL-mode tenant under a `cr_12306_` table prefix. Added a Tencent CVM runbook recommending app/API hosting on 80/443, private OceanBase on localhost/VPC or SSH tunnel, and Mapbox hosted styles/tiles with public token only.
Tests run: `python3 -m py_compile scripts/migrate_12306_to_oceanbase.py scripts/oceanbase_seed.py scripts/oceanbase_booking_ingest.py` (passing); `npm run 12306:review` (passed, wrote ignored `exports/12306-oceanbase` artifacts); `node --test tests/12306Migration.test.mjs` (1/1 passing); `npm test` (23/23 passing); `npm run build` (passing with existing chunk-size warning); `rg "sk\\.ey" .` (no matches); `nc -vz -G 5 43.160.208.85 22` (SSH port reachable); `ssh root@43.160.208.85` and `ssh ubuntu@43.160.208.85` in batch mode (both denied).
Result: Passing for local review, migration tooling, docs, tests, build, and secret scan. Live CVM installation/conversion is blocked by SSH authentication: the supplied key is public material and neither `root` nor `ubuntu` accepted the available local keys.
Next steps:
1. Fix SSH access via Tencent console or provide the matching private key/username; then verify CVM resources with `nproc`, `free -h`, and `df -h`.
2. Install OceanBase on the CVM using the documented OBD single-node path if resources are adequate, or Docker quick start for a disposable demo only.
3. Keep OceanBase ports private and run `npm run 12306:migrate -- --create-database --truncate` through an SSH tunnel, then capture `cr_12306_` table counts.

2026-05-13T10:44:15Z | branch: main
Feature/bug worked on: Follow-up Tencent CVM SSH authentication probe.
Files touched: handoff.md, agent-progress.txt.
Summary: Tried SSH password login as `rogerlin@43.160.208.85` using the two user-supplied candidate passwords. Both attempts were rejected by the server. The password values were not written to any project file.
Tests run: SSH interactive login probe only; no application code changed.
Result: Blocked. CVM installation and live OceanBase load still require Tencent console access, a reset/known-good SSH password, or the matching private key.

2026-05-13T10:48:06Z | branch: main
Feature/bug worked on: Corrected Tencent CVM SSH authentication probe.
Files touched: handoff.md, agent-progress.txt.
Summary: Stopped the typo username attempt and tried SSH password login as `ubuntu@43.160.208.85` using the user-supplied candidate password. The server rejected it. The password value was not written to any project file.
Tests run: SSH interactive login probe only; no application code changed.
Result: Blocked. CVM installation and live OceanBase load still require Tencent console access, a reset/known-good SSH password, or the matching private key.

2026-05-13T10:53:00Z | branch: main
Feature/bug worked on: Successful Tencent CVM SSH authentication and resource check.
Files touched: handoff.md, agent-progress.txt.
Summary: After the user reset the password, SSH login as `ubuntu@43.160.208.85` succeeded. The account has passwordless sudo. The server reports Ubuntu 24.04.4 LTS on x86_64, 2 vCPU, 961 MiB RAM, 1.9 GiB swap, and a 40 GB root disk with 32 GB available. The password value was not written to any project file.
Tests run: SSH identity/resource probe only; no application code changed.
Result: Authentication is fixed, but OceanBase installation remains blocked by instance size. The CVM is suitable for static app/API hosting, but it is far below OceanBase's minimum memory guidance. Resize to at least 4 vCPU / 16 GB RAM for a credible single-node OceanBase demo, or use local/managed OceanBase while hosting only the app/API on this VM.

2026-05-13T11:56:19Z | branch: main
Feature/bug worked on: Local OceanBase Desktop 12306 migration and runtime simulation-data rewiring.
Files touched: scripts/migrate_12306_to_oceanbase.py, scripts/export_oceanbase_simulation_data.py, scripts/serve-static.cjs, src/App.jsx, tests/12306Migration.test.mjs, package.json, .env.example, .gitignore, README.md, docs/12306-db-review.md, feature_list.json, handoff.md, public/oceanbase-*.json.
Summary: Found that the current OceanBase Desktop install runs a healthy writable `observer` inside the `oceanbase-desktop` OrbStack VM, while host loopback was not the right endpoint. Verified writes in the VM, installed `python3-pymysql` there, migrated all 255,938 rows from `/Users/rogerlin/Downloads/chinashsr/12306.db` into local OceanBase `chinahsr` tables, and added an exporter/API that converts `cr_12306_route_stations` into simulation-ready ordered route contracts. The static server now serves `/api/oceanbase-simulation-data`, preferring the local Desktop VM, then direct PyMySQL, then the generated `public/oceanbase-simulation-data.json` fallback. The app topbar reports the active source as `OceanBase 12306 (222 routes)`.
Tests run: in-VM `obclient` write probe (passed); in-VM migration load (3,365 stations, 388 train routes, 4,760 route stops, 331 tickets, 1,271 prices, 226,613 tracks, 3,345 locations, 15,865 station-track links, 9.238s); in-VM export (222 high-speed/EMU routes, 481 stations, 509 matched generated-rail segments); `python3 -m py_compile scripts/export_oceanbase_simulation_data.py scripts/migrate_12306_to_oceanbase.py` (passing); `node --check scripts/serve-static.cjs` (passing); `node --test tests/12306Migration.test.mjs` (2/2 passing); `npm test` (24/24 passing); `npm run build` (passing); `./init.sh` (complete, including data prep, tests, build, secret scan); `curl http://127.0.0.1:5174/api/oceanbase-simulation-data` (ok true, source oceanbase-12306, no fallback); Playwright opened `http://127.0.0.1:5174/` and saw `OceanBase 12306 (222 routes)`.
Result: Passing. The local Mac OceanBase Desktop tenant now contains the migrated 12306 reference data, and the browser simulation starts from OceanBase-derived route contracts rather than only static generated JSON.
Next steps:
1. If route geometry needs another fidelity pass, enrich the 1,256 OceanBase-only ordered-stop edges that did not match generated rail segments by snapping them against `cr_12306_railway_tracks`.
2. If the Tencent CVM should host OceanBase later, resize it first; the current 1 GB host remains suitable for app/API hosting only.

2026-05-13T16:56:18Z | branch: main
Feature/bug worked on: OceanBase rail-path geometry fix after user reported haphazard routes and sudden sharp turns.
Files touched: scripts/export_oceanbase_simulation_data.py, tests/oceanbaseRouteGeometry.test.mjs, public/oceanbase-*.json, README.md, docs/12306-db-review.md, feature_list.json, handoff.md, agent-progress.txt.
Summary: Root-caused the remaining map artifacts to geometry quality rather than route ordering. The old export preserved stop order but trusted some bad `cr_12306_station_locations` rows and used sparse ordered-stop fallback chords for unmatched edges; `嘉兴` was exported at `98.3077,39.8127`, which made Hangzhou/Jiaxing/Shanghai routes jump thousands of km. The exporter now validates station coordinates against static station data and nearest linked rail vertices, builds a coordinate-level graph from `cr_12306_railway_tracks` and `cr_12306_station_track_links`, traces ordered station edges over that graph, samples bounded rail corridors only when graph search fails, removes visible backtracking hooks, and only then falls back to generated/static geometry or a bounded chord.
Tests run: `python3 -m py_compile scripts/export_oceanbase_simulation_data.py` (passing); live in-VM export via `orb -m oceanbase-desktop` (222 routes, 481 stations, 1,760 rail-graph edges, 5 rail-corridor edges, 0 ordered-stop fallbacks, 116 coordinate corrections, 5.886s); `node --test tests/oceanbaseRouteGeometry.test.mjs tests/12306Migration.test.mjs` (3/3 passing); `npm test` (25/25 passing); `npm run build` (passing with existing chunk-size warning); `./init.sh` (complete, including data prep, 25 tests, build, secret scan); API smoke on `http://127.0.0.1:5174/api/oceanbase-simulation-data` (ok true, source oceanbase-12306, no fallback, same geometry counts); Playwright map/dashboard smoke (topbar `OceanBase 12306 (222 routes)`, one Mapbox canvas, no relevant console errors, screenshots saved under `/tmp`).
Result: Passing. OceanBase runtime routes now follow validated station-to-station rail geometry instead of bad-coordinate zigzags or long straight fallback chords.
Next steps:
1. If more route-following issues are observed, add a route-audit overlay that draws a selected train's active variant/segment polyline for visual debugging.
2. Keep the current Tencent CVM for app/API hosting only unless it is resized before installing OceanBase.

2026-05-14T00:00:00Z | branch: main
Feature/bug worked on: Performance, security, and operational realism improvements (Phases 1–6).
Files touched: src/visualization/Dashboard.jsx, src/visualization/BookingPanel.jsx, src/simulation_core/SimulationWorkerClient.js, src/simulation_core/SimulationEngine.js, src/algorithms/pricing.js, src/visualization/HSRMap.jsx, scripts/serve-static.cjs, tests/engine.test.mjs.
Summary:
- Phase 1 (Frontend): Memoized topLoads and revenueSeries in Dashboard; removed snapshot.stats.totalBookings from BookingPanel quote deps to stop re-quoting on every network booking; added 30-second worker call timeout in SimulationWorkerClient to prevent UI hangs.
- Phase 2 (Engine snapshot): Filter before serialize — only ~400–500 visible candidates instead of all 6000 trains; cached booking options by routeStopSequenceHash to avoid duplicating stop arrays thousands of times.
- Phase 3 (Security/infra): Gzip static assets in serve-static.cjs; fixed XSS vulnerability in Mapbox popups via escapeHtml helper.
- Phase 5 (Dynamic pricing): bookingVelocity Map per routeId, decays 10% every 12 ticks, feeds velocityBoost into priceQuote (max +4.5% surge).
- Phase 6 (Deferred no-shows): No-show decisions moved from booking time to departure time in processStation() for realism. Manual booking.noShow override preserved so tests can still force no-shows deterministically.
Tests run: `npm test` (25/25 passing); `npm run build` (passing); `./init.sh` (complete).
Result: Passing. All 6 improvement phases implemented; deferred no-shows with test override verified.
Next steps:
1. Phase 4 (Pre-scheduled service days) is still skipped — requires larger refactor of advanceServiceDay() to build tomorrow's trains in background chunks.
2. Review COMPREHENSIVE_BUG_AUDIT.md and PERFORMANCE_AUDIT.md for the next highest-impact items.

2026-05-14T01:00:00Z | branch: main
Feature/bug worked on: Comprehensive audit fix sweep — ~45 commits addressing 80+ issues from COMPREHENSIVE_BUG_AUDIT.md.
Files touched: src/simulation_core/SimulationEngine.js, src/algorithms/pricing.js, src/algorithms/seatInventory.js, src/visualization/Dashboard.jsx, src/visualization/BookingPanel.jsx, src/visualization/HSRMap.jsx, src/simulation_core/SimulationWorkerClient.js, src/App.jsx, scripts/serve-static.cjs, scripts/oceanbase_seed.py, scripts/oceanbase_booking_ingest.py, scripts/migrate_12306_to_oceanbase.py, scripts/export_oceanbase_simulation_data.py, scripts/prepare-data.cjs, init.sh, tests/engine.test.mjs, package.json.
Summary:
- Simulation engine: deferred no-shows, monotonic guard epsilon, booking indexes, stale trainId guard, leap-year calendar, live demand filter, empty segmentMinutes guard, nullish segment distance, event counter, train retention across days, average load weighting.
- Pricing: minimum price floor ¥5, minimum forecast floor 0.1.
- Seat inventory: ticketId index for O(1) releaseTicket.
- Frontend: memoized Dashboard, debounced BookingPanel, worker timeout, XSS fix, null handlers on terminate, workerRef cleanup, mergeSnapshot clone, fetch timeout, animation throttle, unique keys, meter accessibility.
- Static server: gzip, path traversal hardening, missing MIME types, OrbStack config, stale fallback rejection, generic errors, O(n²) concat fix, 5-min cache TTL.
- Python scripts: connection cleanup, batch commits, full-field upsert, failed-ingest retention, reconnect during multiprocessing, try/finally around loads, all-or-nothing 12306 load, longer query timeout, visible skip logging, duplicate coordinate warnings, TEXT seats_json, null-safe parsing.
- Data pipeline: directed OD frequency, tighter corridor tolerance, relaxed path rejection, tighter rail corridor sampling, Taiwan region, lower jump threshold, reduced province inference radius.
- Config: Node >=18 engines field, ripgrep fallback in init.sh.
Tests run: `npm run prepare:data` (85.8% rail-traced, 0 straight-fallback); `npm test` (25/25 passing); `npm run build` (passing); `bash -n init.sh` (passing); `python3 -m py_compile` on all .py files (passing).
Result: Passing. Build, tests, data prep, and static smoke checks all green.
Next steps:
1. Review remaining audit items: DP-2 (rail node averaging drift), DP-8 (capVertexCount curvature preservation), SI-1 (group car contiguity), FE-12 (Mapbox token-free fallback).
2. Consider adding integration tests for day-boundary booking retention.
3. Evaluate if the increased repaired-jump count (4654) from DP-5 needs further geometry pipeline tuning.

2026-05-14T01:30:00Z | branch: main
Feature/bug worked on: PERFORMANCE_AUDIT.md optimization sweep — 8 commits reducing snapshot serialization cost and bundle size.
Files touched: src/simulation_core/SimulationEngine.js, src/simulation_core/simulationWorker.js, src/algorithms/seatInventory.js, src/App.jsx, src/visualization/Dashboard.jsx, src/visualization/BookingPanel.jsx.
Summary:
- Exclude booking options from preload snapshots (saves ~9MB × ~50 batches during init).
- Filter candidates before serialize — only running + scheduled within 120min + completed, not all 6000 trains.
- Cache booking options by routeId with minimal {name, index} stops; exclude completed trains from bookingOptions.
- Single-pass candidate filtering eliminates 6000-object intermediate array.
- Single-pass stats computation eliminates 4 intermediate array allocations (averageDelayMinutes, delayedTrains, activeAverageDelayMinutes, activeDelayedTrains).
- serializeTrain stops: { ...stop, index } → { name, index } (9 properties → 2).
- Remove unused classLoads/occupancy from serializeTrain (saves 3-4 occupancyForSegment calls per train).
- Remove 12 unused properties from serializeTrain (type, originalOrigin, originalDestination, originProvince, destinationProvince, legIndex, journeyProgress, progress, departureMinute, delayMinutes, currentDelayMinutes, minutesToDeparture, turnaroundDepartureMinute).
- Cache serialized stops array per train (_serializedStops), invalidated on turnaround.
- Cache calendar object by floor(nowMinutes) — Date and toLocaleString only when minute changes.
- Cache bookings and events arrays by reference equality — reuse when no new entries added.
- Skip snapshot creation in loop() when onUpdate is null (was creating full snapshot every tick via optional-chaining arg eval).
- Eliminate intermediate preload snapshots — only send preload-complete, saving ~49 full serializations during init.
- Halve preload batch size 120→60 for smoother worker message processing.
- Add occupiedOnSegment() to SeatInventory to avoid {occupied, capacity, loadFactor} object allocation in network summary (6000 objects per snapshot).
- Lazy-load Dashboard (373 KB) and BookingPanel (6 KB) with React.lazy + Suspense — main bundle drops from 2330 KB to 1942 KB.
Tests run: `npm test` (25/25 passing); `npm run build` (passing, code-split chunks emitted); `./init.sh` (complete).
Result: Passing. Estimated snapshot object churn reduced from ~64,000 to ~20,000 objects per snapshot. Main bundle 388 KB smaller.
Next steps:
1. Delta snapshots — only send changed train fields instead of full 1500-train array (biggest remaining win, 4-6 hr effort).
2. Background day scheduling — pre-build next day's trains to eliminate ~7s freeze at midnight.
3. Remaining audit items: DP-2 (rail node averaging), DP-8 (capVertexCount curvature), SI-1 (group car contiguity), FE-12 (Mapbox token-free fallback).

2026-05-14T05:00:00Z | branch: main
Feature/bug worked on: Remaining audit fixes + delta snapshots.
Files touched: src/algorithms/seatInventory.js, scripts/prepare-data.cjs, src/visualization/HSRMap.jsx, src/simulation_core/simulationWorker.js, src/App.jsx, src/simulation_core/SimulationEngine.js.
Summary:
- Fix SI-1: findAllocationGroup now keeps large groups in complete rows within the same car instead of scattering across cars.
- Fix DP-8: capVertexCount uses curvature-aware selection (angle deviation + perpendicular deviation from chord) to preserve sharp turns and junctions.
- Fix DP-2: buildRailNetwork no longer averages node coordinates in cells; keeps first coordinate to prevent drift from actual rail lines.
- Fix FE-12: HSRMap fallback view shown for ANY fatal map error, not just missing tokens.
- PERFORMANCE: Remove synchronous preloadDemand() from advanceServiceDay — eliminates the ~7s day-boundary freeze. Background chunked preload starts within 150ms.
- PERFORMANCE: Delta snapshots — worker only sends trains whose dynamic state changed. Frontend mergeSnapshot merges deltas into previous train state. Full snapshots on init/booking/manual/day-boundary. Estimated ~50% snapshot size reduction.
- PERFORMANCE: _serializedStops cache now applied in serializeTrain too (was only in createBookingOptions).
Tests run: `npm test` (25/25 passing); `npm run build` (passing with code-split chunks); `./init.sh` (complete).
Result: Passing. All 4 remaining audit items fixed. Delta snapshots implemented.
Next steps:
1. Consider GPU-driven train animation (Mapbox custom layer) for smoother map rendering.
2. Consider background pre-scheduling of next-day trains to further reduce day-boundary latency.
3. Monitor real-world snapshot sizes with delta enabled.

2026-05-14T06:00:00Z | branch: main
Feature/bug worked on: Final audit fix pass + delta snapshot robustness.
Files touched: src/simulation_core/simulationWorker.js, src/algorithms/seatInventory.js, src/simulation_core/SimulationEngine.js, scripts/prepare-data.cjs.
Summary:
- Fix SI-3: Remove redundant validateInterval from isSeatAvailable hot path.
- Fix SE-1: Self-correcting loop interval — schedule next frame based on actual processing time instead of fixed 50ms, preventing callback pile-up.
- Fix DP-9: buildRailGeojson uses curvature-aware capVertexCount instead of uniform stride simplification.
- Fix delta snapshot consistency: manual snapshot requests now sync lastPublishedTrains so subsequent tick deltas remain consistent.
Tests run: `npm test` (25/25 passing); `npm run build` (passing).
Result: Passing. All known audit items addressed. Delta snapshots working end-to-end.
Remaining complex items for future sessions:
1. GPU-driven train animation (Mapbox custom layer / deck.gl)
2. Binary snapshot format (flat buffers / transferable ArrayBuffer)
3. Background pre-scheduling of next-day trains to further smooth day boundaries
