# Handoff.md

**Last Updated (UTC):** 2026-06-16T09:30:00Z
**Status:** Complete
**Current Focus:** Audited the codebase across 8 dimensions, implemented the verified bug/perf/robustness fixes (EXECPLAN.md), shipped a public GitHub Pages live demo with a tokenless map fallback, made the README demo videos render inline, and corrected documentation drift. Tests 32/32; main bundle 210 KB (was 1.94 MB); preload probe 56 s (was 144 s).

This document uses MUST, SHOULD, and MAY as defined in RFC 2119.

## 1) Request & Context
- **User's request (paraphrased):** Create a detailed and accurate simulation for China's high-speed rail network, including booking. When a passenger gets off at a station, the seat MUST open up and become available for a later passenger on a non-overlapping segment. The system should include an interactive dashboard and Mapbox-rendered map, create a database of railway stations and routes, and focus on a robust optimized algorithm for booking, seat allocation, and dynamic pricing.
- **Operational constraints / environment:** Work is rooted at `/Users/rogerlin/Downloads/chinashsr`. The requested deliverable lives in `/Users/rogerlin/Downloads/chinashsr/ChinaHSR_Simulation`. Available local datasets include station CSVs, train route CSVs, and HOTOSM railway point/line GeoJSON. The enclosing `/Users/rogerlin` git metadata is broken (`fatal: bad object HEAD`), so this folder carries its own local git repo, harness, and verification script.
- **Guidelines / preferences to honor:** Maintain `handoff.md`, `feature_list.json`, `agent-progress.txt`, and `init.sh`. Work in small, verifiable increments. Do not store the supplied Mapbox secret token in source files. Use the public token and allow override with `VITE_MAPBOX_TOKEN`.
- **Scope boundaries (explicit non-goals):** This implementation does not claim an official real-time 12306 production dataset. It builds a realistic simulation seeded from local station, train, and OSM datasets, with explicit provenance and generated schedules/routes where the local data does not include full stop-by-stop timetables.
- **Changes since start (dated deltas):** 2026-05-02: Started a new deliverable folder after discovering an earlier `sim/` prototype and local source datasets. 2026-05-02T11:06:24Z: Completed the first verified implementation covering data generation, interval-aware booking, dynamic pricing, DES movement, Mapbox visualization, dashboard, tests, and local dev server launch. 2026-05-02T11:17:49Z: Reworked route selection and train scheduling after the user observed route clustering; the database now covers 1,200 simulation routes, 85 origins, 77 destinations, 27 origin provinces, and 27 macro-corridors, while the engine schedules 1,500 train services. 2026-05-02T15:02:00Z: Fixed the refused-connection issue by adding a static production server on fixed port 5174 and submitting it to `launchctl`; added no-show inventory release, planned-vs-actual runtime modeling, station pressure metrics, and dashboard realism panels. 2026-05-02T17:57:01Z: Implemented browser multithreading with a simulation Web Worker, live demand sales, rail-corridor geometry interpolation, smaller train markers, station diamond/square/dot symbology, and dashboard quota/trains-per-route metrics. 2026-05-02T19:43:46Z: User requested a high-volume calendar-aware refinement: at least two trains per route per day, many more trains on trunk/surge routes, Jan 1 24-hour time, holiday/weekend/peak-season demand surges, surge pricing, 5x passenger count, smoother animations, and further algorithm optimization. 2026-05-02T20:19:11Z: Completed that refinement with 6,000 scheduled services, 2.69M+ passengers after preload, 663 active trains in the full probe, Jan 1 New Year demand/price surge, ETL path recovery, Mapbox token fallback repair, and browser verification. 2026-05-03T08:08:00Z: Reopened for the user's "continue" request, carrying forward the prior ask to make the simulation a full year, integrate local OceanBase Desktop, improve multiprocessing/concurrency, and avoid cumulative caps on annual passengers/revenue/trains. 2026-05-11T11:58:13Z: Reopened for robustness review focused on train oscillation, station-skipping/shortcut geometry, and OceanBase integration confidence. 2026-05-11T13:05:41Z: Reopened for the user's follow-up asking to save each route and stop sequence in OceanBase and make terminal trains reverse through the same stations in reverse order instead of ending permanently at the terminal. 2026-05-13T10:12:52Z: Reopened after the user installed pyseekdb/OceanBase Desktop and asked for a thorough `12306.db` review plus a Tencent CVM hosting/OceanBase migration plan. 2026-05-13T11:22:16Z: User redirected the plan to the local MacBook Pro M4 Max OceanBase Desktop instance. The healthy writable endpoint is inside the `oceanbase-desktop` OrbStack VM at `127.0.0.1:2881`; the host-side `127.0.0.1:2881` endpoint was stale/unusable. Live migration of `12306.db` into the local OceanBase `chinahsr` tenant completed successfully.

## 2) Requirements -> Acceptance Checks (traceable)
| Requirement | Acceptance Check (scenario steps) | Expected Outcome | Evidence Captured |
|---|---|---|---|
| R1: Database of stations and routes | Run `./init.sh`, which builds data artifacts from station CSV, train CSV, and OSM GeoJSON. | `public/station-data.json`, `public/route-data.json`, `public/hsr-stations.geojson`, and `public/hsr-rails.geojson` exist and contain non-empty records. | Passed: 3,058 stations, 7,278 HSR service records, 4,640 known endpoint records, 1,200 simulation routes, 85 origins, 27 corridors, 2,200 rail features. |
| R2: Segment-aware seat reuse | Run the booking engine regression test: book a seat A->C, reject overlap B->D for the same seat, allow reuse C->D after alighting. | The same seat can be allocated to non-overlapping intervals and not to overlapping intervals. | Passed in `tests/seatInventory.test.mjs`. |
| R3: Robust booking algorithm | Exercise booking in the app and test engine-level `quoteTrip`/`bookTrip` paths. | Passenger receives a real ticket ID, seat ID, car/row/letter, price, and station interval; capacity and availability update by segment. | Passed in `tests/engine.test.mjs` and production build. |
| R4: Dynamic pricing | Run tests and inspect UI quotes for different demand/time/class conditions. | Price changes with distance, seat class, segment occupancy pressure, booking horizon, route frequency, and peak-hour demand. | Passed in `tests/pricing.test.mjs`; quote panel exposes multiplier and bid-price metadata. |
| R5: Interactive dashboard and Mapbox map | Run `npm run build` and `npm run dev`; open the app locally. | Map renders railway layers and train positions; dashboard shows trains, bookings, revenue, load factors, and event log. | Passed production build; Vite server running at `http://127.0.0.1:5174/`. |
| R6: Secret-safe configuration | Search the new folder for the supplied Mapbox secret token prefix. | No `sk.` token is present in source; public token is used as a browser default and may be overridden with env var. | Passed: `rg "sk\\.ey" .` produced no matches and `./init.sh` secret scan passed. |
| R7: Nationwide scalable service coverage | Run the data diversity and engine scale tests, then instantiate the full engine from generated data. | Routes are not dominated by the first CSV records; booking options cover the full scheduled train set; map rendering is capped to active/near-term trains for performance. | Passed: 1,500 trains, 170 active at initial snapshot, 389 visible/near-term trains, 1,500 booking options, 27 corridors, 27 origin provinces. |
| R8: Stable local serving | Run `npm run build`, start `node scripts/serve-static.cjs`, and check `curl -I http://127.0.0.1:5174/`. | The in-app browser URL returns HTTP 200 on fixed port 5174. | Passed: `launchctl` job `com.codex.china-hsr-simulation` is running with PID 22517 and curl returns HTTP 200. |
| R9: Operational realism | Run tests and instantiate the full engine from generated data. | No-show seats release after departure, delays are modeled, and station pressure/realism metrics are exposed. | Passed: 6 tests; full-engine probe reported 1,500 trains, 181 active, 400 visible, 67 no-show releases, 7.8 min average delay, 1,124 delayed trains, and 16 station hotspots. |
| R10: Parallel processing and multithreading | Build the app and inspect generated chunks. | Simulation engine runs in a browser Web Worker thread; React/Mapbox UI receives snapshots and sends quote/booking commands via message passing. | Passed: build emits `dist/assets/simulationWorker-*.js`; Dashboard reports `simulation-worker`. |
| R11: Live-changing demand metrics | Run the live-demand regression test and a full-engine probe across ticks. | Revenue and passengers increase during simulation ticks instead of staying at preload values. | Passed: `tests/engine.test.mjs` live-demand test; probe revenue rose from ¥62,265,625 to ¥62,270,365 and passengers from 407,005 to 407,029 after 16 ticks. |
| R12: Rail-corridor movement and map mark refinement | Regenerate data and inspect segment geometry sources; visually inspect map layers. | Train coordinates interpolate along generated HOTOSM rail-corridor polylines where available; station and train markers are less cluttered. | Passed: 8,000 OSM rail features, 52.7% rail-matched segments, train markers reduced, local/regional/national station marks changed to dots/squares/diamonds. |
| R13: Calendar surge train scheduling | Instantiate the full engine and inspect per-route service counts and calendar metadata. | The simulation starts on January 1, runs through a 24-hour day clock, every route has at least 2 trains/day, trunk routes have many more, and surge day multipliers increase service frequency. | Passed: full probe showed 6,000 trains, 1,200 routes, min 4/median 5/max 7 trains per route, Jan 1 08:20, New Year travel surge, 1.58x demand and 1.28x price surge. |
| R14: 5x passenger volume and optimized processing | Run a full-engine probe after demand preload and live ticks. | Passenger count is at least 5x the prior 407,005 baseline while quote/booking paths remain sub-second and worker-driven. | Passed: full probe showed 2,692,848 passengers, 6.62x prior baseline, 663 active trains, 5.156s Node init, p95 quote latency 0.057 ms, and live ticks increased passengers/revenue. |
| R15: Surge pricing tied to calendar demand | Quote the same route on Jan 1 surge conditions and inspect pricing multipliers. | Pricing exposes a calendar surge multiplier greater than 1 and higher fares during surge periods. | Passed: pricing test covers surge multiplier; quote path passes service calendar demand into demand forecast and `priceQuote`. |
| R16: Smoother map animation cadence | Build the app and inspect worker metadata plus browser behavior. | Worker publishes snapshots at a faster interval, train markers are smaller, and visible motion updates feel less jagged. | Passed: worker cadence is 150 ms, map interpolates between snapshots with throttled GeoJSON updates, train render cap is 850, and browser screenshot shows live map/topbar at 6,000 trains and 2.7M passengers. |
| R17: Full-year uncapped annual totals | Run the OceanBase annual seed for 365 days. | Annual train, passenger, and revenue totals are computed across all days without resetting or imposing a cumulative cap. | Pending: capture seed output and public summary JSON. |
| R18: OceanBase integration | Connect to local OceanBase Desktop `chinahsr`, create schema, and bulk load generated records. | OceanBase tables contain station, route, stop, segment, simulation run, and daily route service records. | Blocked externally on 2026-05-11: `.env` has OB fields but `127.0.0.1:2881` refused connection; script now exits with a clear operator message instead of a traceback. |
| R19: Multiprocessing/concurrency | Run the annual generator with multiple CPU workers. | Seed output reports worker count and generated rows, with chunks processed in parallel before bulk insert. | Pending: capture seed output. |
| R20: Dashboard annual evidence | Open the dashboard after build/reload. | UI shows latest OceanBase annual run totals and hardware/concurrency metadata. | Pending: capture DOM or browser smoke check. |
| R21: Monotonic train route progression | Run an engine regression that advances a train across multiple ticks and asserts current segment index never decreases, station indexes are processed once, and destination completion happens exactly once. | Trains cannot oscillate between two stations or regress after advancing along a route. | Passed: `train movement is monotonic and processes every crossed station once`; `npm test` 20/20. |
| R22: No direct station shortcut geometry | Regenerate data and run geometry validation over every segment and merged route geometry. | Segment geometries begin/end at their station endpoints, contain no huge jumps, and long routes preserve multi-point rail/corridor geometry instead of single direct chords except explicitly bounded short fallback cases. | Passed: 7,338 segments, 82.8% rail-traced, 100.0% rail-matched, 0 long straight fallback, 0 endpoint mismatches, 0 >90 km single-hop shortcuts. |
| R23: OceanBase live ledger resilience | Exercise booking ledger drain/POST/ingest paths without requiring credentials and inspect server health metadata. | The HTTP server records ledger batches durably, reports queued/pending/failed counts, and only dispatches OceanBase loading when credentials are present. | Passed: ingest dry-run validates 1 good / skips 2 malformed rows; `/healthz` and `/ledger-stats` report pending file/byte metadata; POST `/ingest-bookings` wrote an NDJSON batch when ingest disabled. |
| R24: OceanBase annual schema confidence | Run `oceanbase_seed.py --skip-db` and inspect generated summary/table metadata. | Annual route-day generation remains uncapped, parallel, deterministic enough for tests, and emits useful OceanBase table/architecture metadata for the dashboard. | Passed: 7-day skip-db smoke generated 8,400 route-day rows, 110,932 trains, 52,644,235 passengers, ¥22,627,037,838.91 revenue with 2 workers; test suite also covers 30-day skip-db. |
| R25: Queryable OceanBase route variants | Run the OceanBase seed in skip-db and schema/summary checks, then inspect the table metadata and sample SQL in documentation. | OceanBase schema includes outbound and return route variants, ordered variant stops, variant segments, variant geometry, and raw rail-track geometry so SQL can answer which stations a route traverses in either direction. | Passed: skip-db summary reports 2,400 route variants, 17,076 variant-stop rows, 14,676 variant-segment rows, 14,676 variant-geometry rows, and 12,000 raw rail tracks; README includes route-variant SQL. |
| R26: Terminal turnaround return trips | Run an engine regression that advances a train from origin to terminal, through turnaround dwell, and back to the original station. | The same train switches from outbound to return direction at the terminal, uses the same station sequence in reverse order, processes each return station once, and only becomes completed after returning to the original station. | Passed: `train reverses at the terminal and returns through the same stations in reverse order`; full `./init.sh` passed. |
| R27: Route contract invariants | Run route-data and engine tests that assert every generated route carries a stable ordered stop sequence and every train exposes its active route variant/direction. | The simulation can validate route order before movement, and map/booking snapshots can identify whether a train is following the outbound or return variant. | Passed: `every generated route has an ordered outbound and return route contract`; snapshots expose `routeVariantId`, `direction`, and `journeyProgress`. |
| R28: Thorough `12306.db` review | Inspect SQLite metadata, tables, row counts, indexes, relationships, data samples, and quality risks. | A reproducible review artifact explains what the database contains and how it maps to the simulation/OceanBase schema. | Passed: `npm run 12306:review` inspected 255,938 rows, integrity `ok`, and wrote review artifacts; summary captured in `docs/12306-db-review.md`. |
| R29: Tencent CVM OceanBase deployment plan | Review current official OceanBase deployment requirements and produce a safe CVM architecture/runbook. | The runbook explains how to install/configure OceanBase on the CVM, host the simulation, handle ports/security, and avoid storing Mapbox secret tokens client-side. | Passed for planning: official OceanBase/Tencent/Mapbox docs reviewed and `docs/tencent-cvm-oceanbase-runbook.md` created. Live install is blocked by SSH auth. |
| R30: SQLite-to-OceanBase migration path | Design or implement a migration/export path from `12306.db` into OceanBase-compatible tables. | The migration path should be testable locally without secrets and ready to run against the CVM once SSH/database credentials are available. | Passed locally: `scripts/migrate_12306_to_oceanbase.py`, npm scripts, and `tests/12306Migration.test.mjs` verify dry-run DDL/manifest generation without credentials. |
| R31: Local OceanBase Desktop migration | Run the `12306.db` migration against the OceanBase Desktop VM installed on this Mac. | The `chinahsr` OceanBase tenant contains all `cr_12306_` source tables with row counts matching the SQLite review. | Passed live inside OrbStack VM: 3,365 stations, 388 train routes, 4,760 route stops, 331 tickets, 1,271 prices, 226,613 tracks, 3,345 locations, and 15,865 station-track links loaded in 9.238s. |
| R32: Simulation reads from OceanBase route contracts | Start the static server with local OceanBase enabled and request the simulation-data API. | The app receives station and route JSON converted from `cr_12306_route_stations` ordered stop contracts, with static JSON fallback only if OceanBase is unavailable. | Passed: `/api/oceanbase-simulation-data` returned `ok: true`, 222 routes, 481 stations, source `oceanbase-12306`, no fallback; browser topbar reports `OceanBase 12306 (222 routes)`. |
| R33: OceanBase path-data route fidelity | Export `public/oceanbase-simulation-data.json`, run route-geometry validation, and load the browser map. | OceanBase-derived routes use corrected station coordinates and rail-track graph/corridor polylines for ordered station edges; long sparse ordered-stop chords and obvious sharp zigzags are rejected or repaired. | Passed: live OceanBase export now has 1,760 rail-graph edges, 5 rail-corridor edges, 0 ordered-stop fallbacks, 116 coordinate corrections, and the browser topbar reports `OceanBase 12306 (222 routes)`. |
| R34: Repair the 2026-05-14 work-in-progress regressions | Run `npm test` from the dirty working tree. | All previously failing tests pass: stale `_bookingIndexes` no longer hides post-index bookings from boarding/no-show processing, return legs rebuild their booking index, `hsr-rails.geojson` is generated again for the OceanBase `rail_tracks` pipeline, and movement tests fully re-stage trains. | Passed: 30/30 tests (was 20/25), including restored `tests/oceanbaseSeed.test.mjs` rail-track assertions. |
| R35: Working OceanBase live export with graceful degradation | Request `/api/oceanbase-simulation-data` with the OceanBase VM stopped, then with it running. | No 503/ReferenceError: fresh prebuilt export served when <48h old; live export attempted otherwise and persisted back to `public/oceanbase-simulation-data.json` (self-refreshing cache); stale prebuilt served with `X-ChinaHSR-OceanBase-Fallback: stale-prebuilt` when the database is unreachable; stranded booking-ledger NDJSON batches are re-dispatched by a 90s sweep. | Passed: API returned HTTP 200 with `stale-prebuilt` fallback while the VM observer was down (previously a 503 caused by `isLocalHost(host)`/`stdout`/`stderr` ReferenceErrors). |
| R36: Engine hot-path performance under the 6,000-train budget | Run `npm run perf` (scripts/perf-probe.mjs) against generated data. | Tick p95 well under the 50 ms 20 Hz budget at full scale; train allocation never exceeds the daily budget (1,800 routes previously forced 10,800 trains and crashed Node with OOM). | Passed: exactly 6,000 trains, tick p50 0.41 ms / p95 6.55 ms / max 18.1 ms with 1,633 active trains; snapshot p50 3.1 ms / p95 9.9 ms. |
| R37: Dynamic, realistic operations | Run `tests/scenarios.test.mjs` and observe the dashboard. | Seasonal auto-disturbances (thunderstorm/typhoon/snow/equipment) fire deterministically per seed; disruptions slow each affected train exactly once by stretching remaining segments; knock-on delays push scheduled departures back; live demand follows an hour-of-day curve; ~8%-per-cycle live cancellations refund 90% and write `cancelled` ledger rows; off-peak floating-fare discount up to 20%; dashboard buttons inject scenarios through the worker. | Passed: 30/30 (25 original + 5 new tests); scenario controls render in the dashboard control strip. |

## 3) Plan & Decomposition (with rationale)
- **Critical path narrative:** The highest-risk requirement was seat reuse across station intervals. Implementing that as a pure, testable engine first reduced ambiguity before UI polish. Data generation came next so the dashboard and map are backed by reproducible artifacts. The React UI is now a view over verified simulation state.
- **Step 1:** Create the project harness and continuity files. Completed with `handoff.md`, `feature_list.json`, `agent-progress.txt`, and `init.sh`.
- **Step 2:** Build data preparation scripts that convert local station, train, and OSM datasets into app-ready JSON/GeoJSON. Completed in `scripts/prepare-data.cjs`.
- **Step 3:** Implement booking, seat interval allocation, and pricing as pure modules with regression tests. Completed in `src/algorithms` and `tests`.
- **Step 4:** Implement simulation engine and React dashboard/map integration. Completed in `src/simulation_core` and `src/visualization`.
- **Step 5:** Update progress artifacts, mark features passing only after tests and build passed, and document next work. Completed.
- **Decision log reference(s):** 2026-05-02 decision: never store the supplied secret Mapbox token; use only public browser token and env override.

## 4) To-Do & Progress Ledger
- [x] Create `handoff.md` in the requested new folder — **done**; evidence: this file.
- [x] Create project harness files (`feature_list.json`, `agent-progress.txt`, `init.sh`) — **done**; evidence: `./init.sh` completed.
- [x] Implement data preparation from local station/route/OSM datasets — **done**; evidence: generator counts in R1.
- [x] Implement interval-aware seat allocation and dynamic pricing — **done**; evidence: 3 passing Node tests.
- [x] Implement interactive React dashboard and Mapbox map — **done**; evidence: Vite production build and local dev server.
- [x] Run quality gates and update documentation — **done**; evidence: `./init.sh` completed and README/feature list updated.
- [x] Implement calendar surge/high-volume operations — **done**; evidence: 6,000 trains, 2.69M+ passengers, Jan 1 surge, browser screenshot, and Dashboard metrics verified.
- [x] Review and harden train route progression / geometry / OceanBase integration — **done**; evidence: `./init.sh` complete, 20/20 tests, production build, secret scan clean, LaunchAgent running, `/healthz` HTTP 200, OceanBase live DB connection refusal documented cleanly.
- [x] Add OceanBase route variant and raw rail-track persistence — **done**; evidence: `scripts/oceanbase_seed.py` schema/load path, skip-db summary routeContract counts, Dashboard fallback metrics, README SQL.
- [x] Implement terminal turnaround return trips — **done**; evidence: focused engine regression and 22-test suite.
- [x] Update docs and continuity artifacts — **done**; evidence: README.md, README.zh-CN.md, feature_list.json, handoff.md, and agent-progress.txt.
- [x] Review `12306.db` thoroughly — **done**; evidence: `npm run 12306:review`, `/tmp/chinahsr-12306-review/manifest.json`, and `docs/12306-db-review.md`.
- [x] Design Tencent CVM + OceanBase deployment — **done for the runbook**; evidence: `docs/tencent-cvm-oceanbase-runbook.md`; live execution blocked by SSH auth.
- [x] Build/verify SQLite-to-OceanBase migration path — **done locally**; evidence: `scripts/migrate_12306_to_oceanbase.py`, `tests/12306Migration.test.mjs`, and 23/23 tests.
- [x] Rewire runtime simulation data to local OceanBase Desktop — **done**; evidence: `/api/oceanbase-simulation-data` returns 222 OceanBase 12306 routes and the browser topbar shows `OceanBase 12306 (222 routes)` at `http://127.0.0.1:5174/`.
- [x] Repair OceanBase route geometry fidelity — **done**; evidence: regenerated live OceanBase export, `tests/oceanbaseRouteGeometry.test.mjs`, `./init.sh`, API smoke, and Playwright map/dashboard smoke.

## 5) Findings, Decisions, Assumptions
- **Finding:** An older `sim/` prototype exists, but its booking panel returns a hardcoded seat and does not prove interval-safe seat reuse. The new folder borrowed the useful Mapbox/data shape and replaced the booking core with a tested interval-calendar engine.
- **Finding:** The enclosing git state is broken with `fatal: bad object HEAD`; this folder has its own local git repo for continuity.
- **Decision:** Use a deterministic booking core with seat calendars keyed by segment intervals. A seat is available for `[originIndex, destinationIndex)` only when no existing booking interval overlaps it.
- **Decision:** Generated route records keep all HSR OD records and expose 280 simulation-ready routes. Intermediate stop lists are generated from geographically plausible stations between endpoints and labeled as simulation-derived.
- **Decision:** Replaced first-record route slicing with stratified route sampling by macro-corridor and origin province. The generated route set now exposes 1,200 simulation-ready routes.
- **Decision:** The simulation engine now creates 1,500 scheduled train services from the route set, initializes movement state immediately, returns full lightweight `bookingOptions`, and caps visualized trains to active/near-term/completed services for browser performance.
- **Decision:** Use a tiny `scripts/serve-static.cjs` server for the in-app browser because detached Vite processes are killed by the desktop tool environment. The stable browser URL remains `http://127.0.0.1:5174/`.
- **Decision:** Model realistic operating variance with station dwell, hub/platform pressure, deterministic weather/dispatch delay, no-show probability, and no-show seat release after departure.
- **Decision:** Move simulation execution to a browser Web Worker. The UI no longer owns the `SimulationEngine`; it talks to `SimulationWorkerClient`, which posts `quoteTrip`, `bookTrip`, `setSpeed`, and `snapshot` messages to the worker.
- **Decision:** The current rolling-stock quota is 554 seats per train: 10 business seats, 204 first-class seats, and 340 second-class seats from the configured eight-car layout. With 1,500 trains over 1,200 routes, the current generated schedule averages 1.25 trains per route.
- **Decision:** Use HOTOSM rail-corridor point matching to generate segment polylines. Some segments still fall back to station chords when the local OSM rail subset lacks nearby usable geometry, but the train interpolation now follows rail-corridor polylines for matched segments.
- **Decision:** The scheduling model now treats January 1 as an explicit surge calendar day, keeps a 24-hour operating clock, guarantees at least 2 trains per route per day, and scales additional route services by route frequency, hub endpoints, distance, corridor importance, weekend/holiday/peak-season multipliers, and capacity pressure.
- **Decision:** The high-volume booking path uses cached class pools, segment-load indexes, fast capacity checks, and fast silent allocation for preload/live demand so passenger volume can rise sharply without making every quote scan and sort all seats unnecessarily.
- **Decision:** Keep the production default at 6,000 scheduled trains instead of 8,400. The 8,400-train probe produced 3.82M passengers and 1,322 active trains before optimization, but the final 6,000-train setting still gives 6.62x passengers, 663 active trains, and much safer browser responsiveness.
- **Decision:** Browser worker initialization now uses progressive background demand preload. The UI receives an initial network snapshot quickly, then passenger/revenue totals stream upward as the worker books synthetic demand in chunks.
- **Decision:** Restore the supplied public Mapbox token and style as browser fallbacks while continuing to reject the secret `sk.` token in `./init.sh`.
- **Decision:** For the full-year request, keep detailed seat calendars in the rolling active-day Web Worker and persist the 365-day network plan as OceanBase aggregate facts. This satisfies uncapped annual train/passenger/revenue totals without forcing millions of train objects and seat calendars into the browser heap.
- **Decision:** Use PyMySQL and OceanBase's MySQL-compatible interface for ETL because the local OceanBase Desktop tenant is already reachable as MySQL 5.7-compatible OceanBase at `127.0.0.1:2881`.
- **Decision:** Use CPU multiprocessing for annual planning. The workload is branch-heavy and database-I/O-heavy; Mapbox remains GPU/WebGL-backed for rendering, while compute GPU offload is deferred until there is a regular numeric kernel worth moving to WebGPU.
- **Decision:** For the 2026-05-11 robustness slice, treat train motion as a monotonic route-state machine and treat generated segment geometry as an acceptance artifact. Any train marker interpolation must be derived from per-segment geometry that starts and ends at the station endpoints.
- **Decision:** Add `routeProgress` to serialized trains and guard `updateTrain()` against progress regression. Map interpolation now snaps on route-progress regression, multi-segment jumps, or large coordinate jumps instead of drawing a temporary visual chord.
- **Decision:** Cache polyline arc-length metrics in `geo.js` with a `WeakMap`, so repeated train interpolation over shared route geometries does not recompute segment lengths every snapshot.
- **Decision:** Make OceanBase ingest more operationally observable: `/healthz` now nests ledger queue stats, `/ledger-stats` exposes the same queue directly, `flushLedger()` requeues on non-2xx HTTP responses, and `oceanbase_booking_ingest.py --dry-run` validates/rejects malformed ledger rows without requiring credentials.
- **Decision:** Add `logs/` to `.gitignore` and remove the generated untracked `logs/` directory because it contained prior prompt/tool logs with secret-looking tokens and caused `./init.sh` secret scan to fail.
- **Decision:** Treat the route stop sequence as a persisted contract. OceanBase will store both the canonical generated route and directional variants, with outbound stops in generated order and return stops in exact reverse order. The engine will assign trains to one active variant at a time and will not infer the next station from proximity.
- **Decision:** Implement train turnaround as a two-leg lifecycle inside a single train object: outbound leg, terminal turnaround dwell, return leg, then final completion at the original station. Return bookings use a fresh seat inventory because all outbound passengers must have alighted at the terminal.
- **Finding:** The local OceanBase port remains closed (`nc -z 127.0.0.1 2881` returned exit 1), so real table row-count evidence still requires starting OceanBase Desktop. The code path is covered by Python compilation, schema review, skip-db summary generation, and table-count plumbing.
- **Decision:** Treat the user-provided `sk.` Mapbox token as secret material. It MUST NOT be committed, logged into project artifacts, exposed to browser code, or repeated in documentation. The app may use the provided public `pk.` token via environment variables or server configuration.
- **Finding:** The new review source is `/Users/rogerlin/Downloads/chinashsr/12306.db`. It is outside the `ChinaHSR_Simulation` repo and must be treated as input data until the migration path is designed.
- **Finding:** `12306.db` passed SQLite integrity check and contains 255,938 reviewed rows: 3,365 stations, 388 train routes, 4,760 ordered route stops, 331 tickets, 1,271 ticket prices, 226,613 rail LineStrings, 3,345 station locations, and 15,865 station-track links.
- **Finding:** The route stop data is high value for the user's oscillation concern: 388/388 routes have stops, with zero duplicate station-order routes, zero non-1 starts, zero order-gap routes, and zero unmatched route-station names by `stations.station_name`.
- **Finding:** SSH port 22 on `43.160.208.85` is reachable, but both `root` and `ubuntu` passwordless login attempts failed with `Permission denied (publickey,password)`. The public key string supplied by the user is not enough to install OceanBase from this machine.
- **Finding:** A follow-up password login attempt as `rogerlin@43.160.208.85` using the two user-supplied candidate passwords was also rejected. The passwords were not written to any project file.
- **Finding:** A corrected follow-up password login attempt as `ubuntu@43.160.208.85` using the user-supplied candidate password was also rejected. The password was not written to any project file.
- **Finding:** After the user reset the CVM password, SSH login as `ubuntu@43.160.208.85` succeeded. The account has passwordless sudo. The server is Ubuntu 24.04.4 LTS, x86_64, 2 vCPU, 961 MiB RAM, 1.9 GiB swap, and a 40 GB root disk with 32 GB available.
- **Finding:** The current CVM is not sized for OceanBase. Official guidance reviewed earlier requires at least 6 GB memory for test/experience deployments and materially more for durable production-like use. This host can serve a static app or lightweight API, but should not run OceanBase.
- **Decision:** Keep 12306-derived OceanBase tables under the `cr_12306_` prefix and add query views for route stop sequences, route edges, and ticket-route coverage so the new reference data complements rather than overwrites generated simulation tables.
- **Decision:** The CVM deployment should expose only the app/API on 80/443. OceanBase 2881/ODP 2883/OBD Web 8680/Grafana 3000 should stay private or accessible only via SSH tunnels.
- **Finding:** The current OceanBase Desktop app uses an OrbStack VM named `oceanbase-desktop`. Inside that VM, `/usr/bin/observer` is running with `--port=2881`, 2 GB OceanBase memory, and 2 GB log disk. The VM has 15 GiB memory and 52 GB available disk.
- **Finding:** Host-side `127.0.0.1:2881` was not the healthy endpoint for the current app install. The writable database is reachable from inside the VM with `obclient -h127.0.0.1 -P2881 -uroot`.
- **Decision:** Run local OceanBase migration/export commands inside the `oceanbase-desktop` VM when the host cannot reach port 2881. The Mac filesystem is mounted into the VM, so the same repo path and `12306.db` path are available there.
- **Decision:** Runtime data loading now queries OceanBase first through `GET /api/oceanbase-simulation-data`. The server uses the OrbStack VM path for local OceanBase Desktop, falls back to direct PyMySQL when configured for another host, and finally falls back to `public/oceanbase-simulation-data.json` so the app still starts during temporary database downtime.
- **Decision:** Booking ledger ingestion now follows the same local OceanBase Desktop path. The server writes ledger batches under `.runtime/ledger` so OrbStack can see them, then invokes `oceanbase_booking_ingest.py` inside the VM with an explicit local empty-password flag; the ingest script creates the `bookings` table if it is missing.
- **Finding:** The current OceanBase 12306 export contains 222 high-speed/EMU routes and 481 stations after filtering to G/D/C classes and requiring coordinates. The first route contract is `北京 -> 明光 -> 南京 -> 常州 -> 上海`, with the return contract `上海 -> 常州 -> 南京 -> 明光 -> 北京`.
- **Finding:** The remaining haphazard train paths are not an ordering failure; they are a geometry-quality failure. The current exporter preserves OceanBase stop order, but some station coordinates from `cr_12306_station_locations` are wrong and some station-to-station edges still fall back to sparse straight polylines instead of querying `cr_12306_railway_tracks` / `cr_12306_station_track_links`.
- **Decision:** The OceanBase export validates station coordinates against static station data and linked rail-track anchors, then builds segment geometry from the migrated OceanBase rail-track graph before falling back to existing generated geometry or a bounded densified chord.
- **Decision:** Use nearest linked rail vertices, not linked-track centroids, when validating a station against track links. A test fixture caught that centroids can overcorrect a valid station when the linked LineString is long.
- **Assumption:** Local CSV route records provide real origin/destination train services but not full stop-by-stop timetables. This remains true until a more authoritative timetable source is added.
- **Finding (2026-06-11):** The working tree contained a large uncommitted 2026-05-14 WIP (IMPROVEMENT2.md implementation): MIN_DAILY_TRAINS_PER_ROUTE 2→6, route data regenerated to 1,800 routes, `hsr-rails.geojson` generation disabled, prebuilt-export short-circuit added to the server. Five tests failed and the live OceanBase export endpoint always threw ReferenceErrors (`isLocalHost(host)`, `stdout`/`stderr` never built from chunks).
- **Finding (2026-06-11):** `bookTrip` pushed to `train.bookings` without updating the cached `_bookingIndexes`, so any booking made after a train's first station processing never boarded/alighted/no-showed; `prepareReturnLeg` also kept the outbound index for the fresh return leg. Both real engine bugs, now fixed (incremental index maintenance + index reset at turnaround).
- **Finding (2026-06-11):** With 1,800 routes the 6-train floor forced 10,800 trains (floor overrode the budget), crashing Node with OOM during preload — the browser worker would hit the same wall on static-fallback data. `effectiveMinTrainsPerRoute()` now scales the floor to the budget: 222 OceanBase routes keep 6/route, 1,800 synthetic routes degrade to 2/route, and the budget is honored at exactly 6,000 trains.
- **Decision (2026-06-11):** Disruption scenarios apply a one-time stretch to each affected train's remaining segments (severity 0.6-1.4x) instead of per-tick delay accumulation, which compounded ~400 minutes of delay per real minute at 20 Hz. Knock-on propagation now also pushes scheduled departures back and only fires when a train's delay has grown >=3 min since its last propagation.
- **Decision (2026-06-11):** The server treats `public/oceanbase-simulation-data.json` as a self-refreshing cache: fresh (<48h) prebuilt is served directly, successful live exports overwrite it atomically, and a stale prebuilt is served (flagged via header + `exportAgeHours`) rather than 503ing into the static dataset. Stranded ledger NDJSON batches are re-dispatched every 90s; the ingest deletes inputs on success and upserts idempotently, so retries are safe.

## 6) Issues, Mistakes, Recoveries
- **Issue:** Prior prototype hardcoded booking confirmation. **Root cause:** UI was implemented before a real booking API. **Fix:** Exposed `quoteTrip`, `bookTrip`, and `cancelBooking` from the engine and routed UI bookings through them. **Guardrail:** `tests/engine.test.mjs` fails if booking does not mutate segment availability.
- **Issue:** The parent git repository is unusable. **Root cause:** `/Users/rogerlin/.git` reports `fatal: bad object HEAD`. **Fix:** Initialize and commit inside `ChinaHSR_Simulation` rather than attempting to repair unrelated parent history.
- **Issue:** User observed that most routes were not being travelled and trains clustered on a few corridors. **Root cause:** `prepare-data.cjs` selected the first 280 eligible route records and `SimulationEngine` only instantiated the first 120 routes. **Fix:** Added diversified route selection, 1,500 scheduled services, immediate movement-state initialization, full booking options, and dashboard coverage charts. **Guardrail:** `tests/dataDiversity.test.mjs` and the engine scale test fail if coverage collapses.
- **Issue:** User reported `ERR_CONNECTION_REFUSED` for `127.0.0.1:5174`. **Root cause:** No server was listening; detached Vite child processes exit in this environment and the prior port was not pinned by default. **Fix:** Pin Vite dev/preview to 5174, add a static production server, and submit it via `launchctl` under `com.codex.china-hsr-simulation`. **Guardrail:** `curl -I http://127.0.0.1:5174/` must return HTTP 200 before ending.
- **Issue:** User observed frozen revenue/passenger/delay metrics. **Root cause:** demand was mostly preloaded at startup and delay metrics used whole-schedule aggregates. **Fix:** added live demand sales every simulation ticks and active-train delay metrics. **Guardrail:** live-demand regression checks revenue/passenger totals increase after ticks.
- **Issue:** User observed trains travelling over water. **Root cause:** positions used straight interpolation between station coordinates. **Fix:** generated HOTOSM rail-corridor segment polylines and interpolate train coordinates along those polylines when available. **Guardrail:** data diversity test requires at least 45% rail-matched segments and detailed rail polylines.
- **Issue:** A real OceanBase smoke could not connect on 2026-05-11. **Root cause:** OceanBase Desktop / MySQL-mode tenant was not listening on `127.0.0.1:2881`. **Fix:** `oceanbase_seed.py` now catches `pymysql.err.OperationalError` and exits with a concise operator message. **Guardrail:** skip-db annual generation and ingest dry-run remain green without requiring a live database.
- **Issue:** `./init.sh` secret scan failed on untracked generated logs. **Root cause:** `logs/` contained prior prompt/tool logs with a secret-looking Mapbox token. **Fix:** removed the generated log folder and ignored `logs/` going forward. **Guardrail:** rerunning `./init.sh` now completes through secret scan.
- **Issue:** Tencent CVM install could not proceed. **Root cause:** SSH authentication is not configured for the available local keys/users; only the server IP and a public key were supplied. **Fix:** stop before any destructive or speculative remote action and provide a local runbook plus SSH-tunnel migration path. **Guardrail:** OceanBase ports stay private in the deployment plan.
- **Issue:** Follow-up password login as `rogerlin` also failed. **Root cause:** The supplied candidate credentials are not accepted by the CVM for SSH. **Fix:** Stop after clean denial and keep remote install/load blocked until Tencent console access, a reset password, or the matching private key is available.
- **Issue:** Follow-up password login as `ubuntu` also failed. **Root cause:** The supplied candidate credential is not accepted by the CVM for SSH. **Fix:** Stop after clean denial and keep remote install/load blocked until Tencent console access, a reset password, or the matching private key is available.
- **Issue:** CVM SSH access is now fixed, but OceanBase cannot be installed responsibly on the current instance size. **Root cause:** The host has only 961 MiB RAM and is already using swap; OceanBase minimum test guidance is multiple GB of RAM. **Fix:** Defer OceanBase install until the CVM is resized or use local/managed OceanBase, while keeping the CVM suitable for static app/API hosting.
- **Issue:** Local host connection to OceanBase Desktop failed or hit stale `seekdb` errors. **Root cause:** The active current install runs OceanBase inside an OrbStack VM, while a stale trashed host-side SeekDB process had previously accepted reads but failed writes with `ob clog disk hang`. **Fix:** Use the current VM-local `observer` endpoint through `orb -m oceanbase-desktop`, verify writes with a probe table, and run the migration from inside the VM. **Guardrail:** Future local DB checks should verify both `orb list` and an in-VM `CREATE TABLE` probe, not only host `nc 127.0.0.1 2881`.

- **Issue (2026-06-11):** `/api/oceanbase-simulation-data` returned 503 whenever the prebuilt export aged past 48h. **Root cause:** three ReferenceErrors in `serve-static.cjs` (`isLocalHost(host)` with `host` undefined; `stdout`/`stderr` used in `runJsonExport` without assembling them from the collected chunks). **Fix:** correct variable scoping and build stdout/stderr from chunk buffers. **Guardrail:** `node --check` in verification plus the live API smoke; degradation now serves stale prebuilt data instead of 503.
- **Issue (2026-06-11):** Node OOM crash during the full-engine perf probe. **Root cause:** per-route minimum (6) times 1,800 routes overrode the 6,000-train budget, allocating 10,800 trains x 554-seat inventories. **Fix:** `effectiveMinTrainsPerRoute()` scales the floor to the budget. **Guardrail:** `per-route service floor respects the daily train budget` test + `npm run perf` completing at exactly 6,000 trains.
- **Issue (2026-06-11):** Delta snapshots that carried refreshed bookingOptions replaced the UI train list with only the changed trains, making unchanged map markers vanish. **Root cause:** `mergeSnapshot` treated any snapshot with bookingOptions as full. **Fix:** merge on `delta: true` regardless, adopting refreshed options when present.

## 7) Scenario-Focused Resolution Tests (problem-centric)
- **Requested change:** Seat opens after passenger gets off. **Repro steps:** Create a route A-B-C-D, allocate seat for A-C, attempt B-D, then allocate C-D. **Post-change behavior:** B-D cannot use the occupied seat, but C-D reuses the same physical seat because intervals touch but do not overlap. **Verdict:** Resolved by `SeatInventory`.
- **Requested change:** Dynamic pricing. **Repro steps:** Quote the same OD/class under low and high occupancy. **Post-change behavior:** High-occupancy quote is higher, and first/business class quotes exceed second class. **Verdict:** Resolved by `priceQuote`.
- **Requested change:** Interactive dashboard and Mapbox map. **Repro steps:** Run `npm run dev -- --port 5173`, allow Vite to select an open port, and request the page. **Post-change behavior:** Server responds `HTTP/1.1 200 OK` at `http://127.0.0.1:5174/`; app loads generated station/route data and uses Mapbox GL with the public token/style.
- **Requested change:** More comprehensive nationwide route travel. **Repro steps:** Run `npm run prepare:data`, inspect diversity counts, instantiate the engine from generated data, and inspect snapshot stats. **Post-change behavior:** Data contains 1,200 routes across 85 origins and 27 corridors; engine instantiates 1,500 trains, has 170 active at initial snapshot, exposes 1,500 booking options, and caps visible trains to 389 active/near-term services. **Verdict:** Resolved for the current local data source.
- **Requested change:** Site must load in the in-app browser. **Repro steps:** Start the static server via `launchctl`, then run `curl -I http://127.0.0.1:5174/`. **Post-change behavior:** Server returns HTTP 200 and `launchctl print` reports state `running`. **Verdict:** Resolved.
- **Requested change:** Make the simulation more realistic. **Repro steps:** Run tests and a full-engine scale probe. **Post-change behavior:** No-show bookings release held seat intervals after departure; trains carry planned-vs-actual delay, hub/platform pressure, weather/dispatch delay; dashboard shows average delay, no-show releases, station stops, delayed trains, and station platform pressure. **Verdict:** Resolved for this increment.
- **Requested change:** Implement parallel processing and multithreading. **Repro steps:** Build the app and inspect emitted assets. **Post-change behavior:** Vite emits a separate `simulationWorker-*.js`; the UI uses `SimulationWorkerClient` message passing and Dashboard shows `simulation-worker`. **Verdict:** Resolved.
- **Requested change:** Reduce visual clutter and answer quota/schedule scale questions. **Repro steps:** Build and inspect dashboard/map. **Post-change behavior:** train markers are smaller; station marks use local dots, regional squares, national diamonds; dashboard shows 554 seat quota per train and 1.25 trains per route. **Verdict:** Resolved.
- **Requested change:** Add many more trains, 24-hour calendar time, surge demand/pricing, 5x passengers, optimized processing, and smoother animation. **Repro steps:** Instantiate full engine from generated data, run `./init.sh`, curl port 5174, open the in-app browser, inspect map and Dashboard. **Post-change behavior:** Full probe showed 6,000 trains, 663 active, 2,692,848 passengers, 6.62x prior passenger baseline, Jan 1 New Year surge, 1.28x price surge, and p95 quote 0.057 ms. Browser screenshot showed 6,000 trains, 639 active, 2,700,124 passengers, Jan 1 Thu 11:47, and New Year travel surge. Dashboard DOM exposes Calendar day, Demand surge, Price surge, Route daily range, Worker cadence, and Passengers booked. **Verdict:** Resolved.
- **Issue:** `./init.sh` failed because the original station CSV folder was absent. **Root cause:** `prepare-data.cjs` used one hardcoded source path. **Fix:** Added source discovery over the current folder and the sibling `chinashsr copy` dataset paths. **Guardrail:** `./init.sh` now passes from a clean ETL run.
- **Issue:** Browser rendered a blank dark page after the performance changes. **Root cause:** the Mapbox public token fallback had become an empty string, so Mapbox threw an access-token error during map mount. **Fix:** restored the public token fallback and user-provided style URL, with secret scan still passing. **Guardrail:** browser reload now shows the app and `rg "sk\\.ey" .` remains clean.
- **Requested change:** Prevent trains oscillating between two stations. **Repro steps:** Run a dedicated engine test that manually advances a train to exact segment boundaries, across multiple segments, and then attempts to move the clock backward. **Post-change behavior:** `routeProgress` never regresses, all station indexes `[0,1,2,3]` process exactly once, and a completed train stays completed. **Verdict:** Resolved by monotonic engine guard and regression test.
- **Requested change:** Prevent trains from taking direct over-water or endpoint-to-endpoint shortcuts. **Repro steps:** Regenerate data and run geometry validation over all segment endpoint anchors and coordinate hops. **Post-change behavior:** 7,338 generated segments have 0 endpoint mismatches, 0 long straight fallback segments, 0 sparse long segments, and 0 >90 km single-hop shortcuts. **Verdict:** Resolved for current OSM-backed data; test now fails future regressions.
- **Requested change:** Improve OceanBase integration. **Repro steps:** Run booking-ingest dry-run, POST an NDJSON booking to `/ingest-bookings`, inspect `/ledger-stats`, run annual skip-db smoke, and attempt real DB seed. **Post-change behavior:** malformed ledger rows are skipped; non-2xx ledger POSTs are retried; server reports queue stats; annual skip-db works; real DB seed fails cleanly because OceanBase is not currently listening. **Verdict:** Code path improved and verified; live DB load requires starting OceanBase Desktop.
- **Requested change:** Save each route and station sequence in OceanBase and reverse trains at terminal stations. **Repro steps:** Run one deterministic train over A-B-C-D with 2-minute segments, advance to the terminal, verify the train flips to route variant `r1:return`, then advance D-C-B-A to original station. **Post-change behavior:** The same train changes origin/destination to D->A, uses reversed segments D->C/C->B/B->A, processes return station indexes `[0,1,2,3]`, and completes only after reaching A. **Verdict:** Resolved.
- **Requested change:** Review `12306.db` thoroughly. **Repro steps:** Run `npm run 12306:review` against the default `../12306.db`. **Post-change behavior:** Script emits `manifest.json`, `schema.sql`, and `sample_queries.sql`; review summary documents integrity, row counts, route-order quality, referential checks, station locations, rail geometry parsing, and integration caveats. **Verdict:** Resolved locally.
- **Requested change:** Convert `12306.db` into an OceanBase database on the CVM. **Repro steps:** Run migration dry-run locally and attempt SSH discovery for the CVM. **Post-change behavior:** Local conversion path is implemented and tested; live CVM load is blocked by SSH authentication, not by migration tooling. **Verdict:** Ready but externally blocked.
- **Requested change:** Decide whether to host on Tencent CVM and maybe host Mapbox there. **Repro steps:** Review official OceanBase/Tencent/Mapbox docs and encode an architecture. **Post-change behavior:** Runbook recommends CVM-hosted app/API + private OceanBase + Mapbox hosted tiles/styles with public token only; self-hosting tiles is documented as a separate MapLibre/vector-tile project, not needed for this simulation. **Verdict:** Resolved as a deployment plan.
- **Requested change:** Use the local MacBook Pro OceanBase Desktop instance and migrate `12306.db` there. **Repro steps:** Locate the active Desktop backend, prove write capability with a throwaway table, install the missing PyMySQL driver in the VM, then run the migration against `chinahsr`. **Post-change behavior:** All eight `cr_12306_` tables loaded with exact expected row counts. **Verdict:** Resolved.
- **Requested change:** Rewire the simulation to query OceanBase route data. **Repro steps:** Start the stable static server, request `/api/oceanbase-simulation-data`, then open `http://127.0.0.1:5174/` in Playwright. **Post-change behavior:** The API returns `ok: true`, 222 routes, 481 stations, source `oceanbase-12306`, no fallback; the browser loads and the topbar reports `OceanBase 12306 (222 routes)`. **Verdict:** Resolved.
- **Reported issue:** Some trains still follow haphazard track paths with sudden sharp turns. **Repro steps:** Inspect the current OceanBase export and route examples such as D12/D57/D11. **Pre-fix behavior:** `嘉兴` exported at `98.3077,39.8127`, causing Hangzhou/Jiaxing/Shanghai routes to jump thousands of km and then jump back. **Post-change behavior:** `嘉兴` exports at `120.758917,30.766542`, D12 is 1,274.0 km instead of 6,247.6 km, D57 is 1,524.1 km instead of 6,475.1 km, and all 1,765 ordered OceanBase route edges use rail graph/corridor geometry with zero long straight fallbacks. **Verdict:** Resolved.

## 8) Verification Summary (evidence over intuition)
- **Fast checks run:** `npm run prepare:data`, `npm test`, `npm run build`, `./init.sh`, `rg "sk\\.ey" .`, `curl -I http://127.0.0.1:5174/`, `curl /healthz`, `curl /ledger-stats`, `python3 scripts/oceanbase_seed.py --skip-db --days 7 --workers 2 --chunk-days 2`, `python3 scripts/oceanbase_seed.py --skip-db --days 365 --workers 4 --chunk-days 8`, `python3 -m py_compile scripts/oceanbase_seed.py scripts/oceanbase_booking_ingest.py`, and focused Node tests for engine/geometry/ledger/route contracts.
- **Fast checks run for 2026-05-11 OceanBase:** Real DB smoke attempted with configured `.env`; connection refused at `127.0.0.1:2881`, now reported cleanly by the script. Credential-free skip-db and live ledger dry-run paths passed.
- **Fast checks run for 2026-05-13 12306/CVM slice:** `python3 -m py_compile scripts/migrate_12306_to_oceanbase.py scripts/oceanbase_seed.py scripts/oceanbase_booking_ingest.py` passed; `npm run 12306:review` passed and wrote ignored export artifacts; `node --test tests/12306Migration.test.mjs` passed; `npm test` passed 23/23; `npm run build` passed; `rg "sk\\.ey" .` returned no matches.
- **Fast checks run for 2026-05-13 local OceanBase slice:** `obclient` inside `oceanbase-desktop` returned `5.7.25-OceanBase SeekDB-v1.0.0.0`; in-VM write probe created/inserted/dropped `chinahsr.codex_write_probe`; `python3-pymysql` was installed in the VM; `scripts/migrate_12306_to_oceanbase.py --load --allow-empty-password --create-database --truncate` loaded all 255,938 rows in 9.238s.
- **Fast checks run for 2026-05-13 runtime rewiring:** `python3 -m py_compile scripts/export_oceanbase_simulation_data.py scripts/migrate_12306_to_oceanbase.py scripts/oceanbase_booking_ingest.py` passed; `node --check scripts/serve-static.cjs` passed; `node --test tests/12306Migration.test.mjs tests/bookingLedger.test.mjs` passed 5/5; `/api/oceanbase-simulation-data` on port 5174 returned 222 routes / 481 stations / source `oceanbase-12306`; Playwright loaded `http://127.0.0.1:5174/` and saw the topbar `OceanBase 12306 (222 routes)`; live POST `/ingest-bookings` inserted a smoke booking into local OceanBase `bookings`, then the smoke row was deleted and `/ledger-stats` reported 0 pending files.
- **Fast checks run for 2026-05-13 route-fidelity fix:** Live in-VM OceanBase export completed in 5.886s with 222 routes, 481 stations, 1,760 rail-graph segments, 5 rail-corridor segments, 0 ordered-stop fallbacks, and 116 coordinate corrections; `node --test tests/oceanbaseRouteGeometry.test.mjs tests/12306Migration.test.mjs` passed 3/3; `npm test` passed 25/25; `npm run build` passed; `./init.sh` passed; API smoke returned `ok: true`, source `oceanbase-12306`, and the same geometry counts; Playwright loaded the map/dashboard with no relevant console errors.
- **Acceptance runs:** `./init.sh` passed, including data prep, 24 Node tests, production build, worker bundle generation, and secret scan. The route-contract 365-day skip-db run wrote `public/oceanbase-yearly-summary.json` with 438,000 route-day rows, 5,538,508 train services, 2,606,735,581 passengers, 2,400 route variants, and 12,000 rail tracks.
- **Performance/latency snapshots (if relevant):** Full-engine probe reported 5.156s Node initialization with synchronous preload, 2.69M passengers, 663 active trains, 6.62x passenger baseline, p95 quote 0.057 ms, and live ticks increasing passengers/revenue. Browser uses progressive preload to keep initial paint responsive.

## 9) Remaining Work & Next Steps
- **Open items & blockers:** Remote CVM SSH access works as `ubuntu`, but live OceanBase install/load on the CVM is blocked by resources: 2 vCPU and 961 MiB RAM are far below OceanBase's practical minimum. Local OceanBase Desktop migration, runtime query wiring, and route-geometry fidelity are complete.
- **Risks:** `12306.db` is a dated snapshot rather than a live authorized 12306 feed. It is excellent for stop sequence, station, geometry, and fare calibration, but ticket coverage remains partial and should not be treated as complete live availability.
- **Next working interval plan:** If further route issues are reported, add a visual route-audit overlay that draws a selected train's active segment polyline and route variant on top of the map. Remote hosting should still keep this 1 GB CVM for app/API only unless it is resized before installing OceanBase.

- **Fast checks run for 2026-06-11 optimization/realism slice:** `npm test` 30/30 (5 baseline failures repaired, 5 new tests added: budget-floor regression, disruption one-time application, demand-surge multipliers, auto-disturbance determinism, hourly demand shape); `npm run build` clean with `simulationWorker-*.js` emitted; `node --check scripts/serve-static.cjs`; `npm run perf` → 6,000 trains, tick p50 0.41 ms / p95 6.55 ms / max 18.1 ms at 1,633 active trains, snapshot p50 3.1 ms / p95 9.9 ms; `launchctl kickstart` restart; `/healthz` ok with ledger ingest enabled; `/api/oceanbase-simulation-data` returned HTTP 200 `stale-prebuilt` with the VM down and a live export once OceanBase Desktop was started.

## 10) Updates to This File (append-only)
- 2026-05-02T10:41:20Z: Created the continuity record with request framing, acceptance criteria, plan, risks, and the first decision log.
- 2026-05-02T11:06:24Z: Completed and verified the simulation slice; updated evidence, progress state, issue resolution, remaining risks, and next-session plan.
- 2026-05-02T11:17:49Z: Added diversified route generation, scalable scheduled services, full booking options, coverage dashboard charts, and diversity/scale regression tests after user reported route clustering.
- 2026-05-02T15:02:00Z: Fixed the refused-connection serving path, added static server and launchctl job, added no-show/delay/station-pressure realism, and verified with 6 tests, build, init, curl, and full-engine scale probe.
- 2026-05-02T17:57:01Z: Added simulation Web Worker multithreading, async booking/quote message bridge, live-demand metric movement, rail-corridor train interpolation, station/train map marker refinements, quota/schedule metrics, and 7-test verification.
- 2026-05-02T19:43:46Z: Reopened the handoff for the user's calendar surge and high-volume scaling request; added R13-R16 acceptance criteria and recorded the intended scheduling/optimization decisions before code edits.
- 2026-05-02T20:19:11Z: Completed the calendar surge/high-volume refinement, fixed ETL path discovery and Mapbox fallback configuration, verified with `./init.sh`, full-engine probe, HTTP 200, browser screenshot, and Dashboard DOM checks.
- 2026-05-03T08:08:00Z: Reopened handoff for the continuation slice; added R17-R20 and recorded the OceanBase annual aggregate design before code edits.
- 2026-05-03T16:55:00Z: Major reliability/robustness pass.
  - Replaced naive projection-by-chord geometry with a 254k-node OSM rail graph + bounded A\* path tracing; geometry source distribution shifted to 70.4 % rail-traced / 29.0 % corridor-sampled / 0.6 % fallback (was 52.7 % matched).
  - Added OSM-based station augmentation (89 missing HSR hubs incl. 西安北/昆明南/南宁东/香港西九龙); known-endpoint coverage rose from 64 % to 96.3 %; Xi'an routes 0 → 67.
  - Route deduplication by directed (origin, destination); 6,186 candidates → 1,850 unique pairs.
  - Douglas-Peucker simplification + 5-decimal rounding shrank `route-data.json` from 78 MB to 13 MB while keeping fidelity (0 big jumps, 0 segment-boundary breaks across 218,127 transitions / 6,301 boundaries).
  - SimulationEngine fixes: process all skipped stations during fast ticks; final-station processing on completion; year-end edge case; deterministic ticket IDs; per-train booking cap; sort key fix in `selectVisibleTrains`; removed dead code.
  - OceanBase: added `route_geometry`, `calendar_summary`, and live `bookings` tables; built browser→server→Python ingest pipeline (`/ingest-bookings` NDJSON endpoint + `oceanbase_booking_ingest.py`); idempotent upserts, backpressure-tolerant retry.
  - Tests: 10 → 16 passing. New geometry-validation, OSM-augmentation, dedup, and booking-ledger regressions.
  - README.md and README.zh-CN.md updated with §5.4 rewrite (rail graph + A\*), schema additions, ingest pipeline diagram, and refreshed metrics.
- 2026-05-11T11:58:13Z: Reopened for the user's robustness/optimization request; added R21-R24 to cover monotonic train progression, no shortcut geometry, live OceanBase ledger resilience, and annual OceanBase summary confidence before making code edits.
- 2026-05-11T12:30:03Z: Completed robustness slice. Added monotonic route-state guard, routeProgress metadata, safe map interpolation snapping, polyline metric caching, ledger HTTP retry-on-non-2xx, ledger queue health endpoints, booking-ingest dry-run validation, clean OceanBase connection failure messaging, `.gitignore` log exclusion, README updates, and 20-test verification.
- 2026-05-11T13:05:41Z: Reopened for route-contract persistence and terminal turnaround return trips; added R25-R27 and recorded the planned engine/OceanBase direction before code edits.
- 2026-05-11T13:35:31Z: Completed route-contract and terminal-return slice. Added `routeContract` metadata to generated routes, OceanBase route variant/variant stop/variant segment/variant geometry/raw rail track tables, Dashboard route-contract metrics, two-leg terminal turnaround state machine, active `routeVariantId`/`direction` snapshot metadata, regression tests, bilingual docs, refreshed annual summary, and full `./init.sh` verification.
- 2026-05-13T10:12:52Z: Reopened for `12306.db` review and Tencent CVM/OceanBase deployment planning. Added R28-R30, recorded Mapbox secret-handling decision, and noted that remote access may need private-key/login discovery.
- 2026-05-13T10:36:19Z: Completed local `12306.db` review, CVM/OceanBase runbook, migration script, npm review/migrate commands, and migration regression test. Marked live CVM install/load blocked on SSH authentication.
- 2026-05-13T10:44:15Z: Tried follow-up password login as `rogerlin@43.160.208.85`; both user-supplied candidate passwords were rejected. No passwords were stored in project files.
- 2026-05-13T10:48:06Z: Tried corrected follow-up password login as `ubuntu@43.160.208.85`; the user-supplied candidate password was rejected. No password was stored in project files.
- 2026-05-13T10:53:00Z: After password reset, SSH login as `ubuntu@43.160.208.85` succeeded. Resource check found Ubuntu 24.04.4, 2 vCPU, 961 MiB RAM, 1.9 GiB swap, and 40 GB disk; OceanBase install is now blocked by instance size rather than credentials.
- 2026-05-13T11:22:16Z: Redirected live database work to local OceanBase Desktop on this Mac. Discovered the healthy writable endpoint is inside the `oceanbase-desktop` OrbStack VM, verified writes, installed PyMySQL in the VM, and loaded all 255,938 `12306.db` rows into the local `chinahsr` OceanBase tenant.
- 2026-05-13T11:56:19Z: Completed runtime rewiring. Added `scripts/export_oceanbase_simulation_data.py`, `/api/oceanbase-simulation-data`, OrbStack-aware local OceanBase querying and booking ingest, static fallback JSON, React source selection, topbar source evidence, docs, tests, live booking-ledger smoke, and full `./init.sh` plus Playwright verification.
- 2026-05-13T16:00:31Z: Reopened for remaining track-path fidelity issues. Added R33 and recorded the coordinate/path-data root cause before code edits: route stop order is preserved, but the exporter still needs station-coordinate validation and OceanBase rail-track graph geometry for ordered stop edges.
- 2026-05-13T16:56:18Z: Completed the route-fidelity fix. The OceanBase exporter now repairs bad station coordinates, traces ordered station edges over the migrated rail-track graph, removes backtracking hooks, regenerates `public/oceanbase-simulation-data.json`, and is covered by a new route-geometry regression plus full `./init.sh` and browser smoke evidence.
- 2026-06-11T15:45:00Z: Optimization/realism/OceanBase pass (R34-R37). Repaired the uncommitted 2026-05-14 WIP regressions (5 failing tests; stale `_bookingIndexes`; missing `hsr-rails.geojson`; budget-overriding train floor that OOM-crashed at 1,800 routes). Fixed the live OceanBase export path (ReferenceErrors + missing `--allow-empty-password` on export and ingest commands) and verified live export (222 routes/481 stations), prebuilt self-refresh, stale-prebuilt degradation, ledger retry sweep, and a booking-ingest round trip into the `bookings` table. Optimized engine hot paths (delay-propagation cooldown with departure pushback, prefix-sum demand sampling, per-minute calendar cache, in-place event log with version counters). Made the simulation dynamic: one-time disruption slowdowns, seasonal deterministic auto-disturbances, hour-of-day demand curve, live cancellations with refunds, floating-fare off-peak discount, and dashboard scenario-injection buttons through the worker protocol. Fixed the delta-merge train-vanish bug and matched map transition duration to the snapshot cadence. Tests 30/30; perf probe: 6,000 trains, tick p50 0.41 ms / p95 6.55 ms.
- 2026-06-16T09:30:00Z: **Audit + optimization + live-demo + media pass** (R38). Ran an 8-dimension parallel code audit (49 findings → 46 confirmed, adversarially verified); see `EXECPLAN.md` for the full list and dispositions.
  - **Perf:** Map engine now loads via dynamic `import()` (mapbox-gl when a `pk.` token is configured, else a tokenless MapLibre + CARTO basemap), splitting ~1.5 MB of GL code out of the entry chunk — **main bundle 1,941 KB → 210 KB (67 KB gzip)**. Seat-allocation fast path (single-seat O(1) return; group early-exit + lazy car grouping) cut the demand-preload probe **144 s → 56 s** (init 6.0 s → 4.3 s). `createBookingOptions` is cached (rebuilt only when dirty); network summary throttled 5→12 ticks.
  - **Bugs:** day-rollover no longer re-preloads (double-books) retained trains (`preloadCursor = retained.length`); `bookingVelocity` counts confirmed sales only; `cancelBooking` reverses passengers/bookings (not just revenue); per-train booking cap releases dropped seats' inventory; deterministic event ids (no `Date.now()`); `priceQuote` fails fast on NaN/negative inputs; `serve-static` stream error handler; `export_oceanbase` `route_code` UnboundLocalError; `prepare-data` `capVertexCount` positional indexing + `findNearestNode` one-extra-ring; `seats_json` DDL → TEXT.
  - **Frontend:** base-path-aware fetches via `src/basePath.js` (`withBase`) + Vite `base` env so the bundle works at `/` and under the Pages sub-path; in-app rail + station GeoJSON layers (self-contained map on any style); React `ErrorBoundary`; dropped the redundant double snapshot on booking; auto-clearing scenario message; non-fatal post-load map errors.
  - **Live demo:** GitHub Pages workflow (`.github/workflows/deploy-pages.yml`) + `VITE_MAPBOX_TOKEN` Actions secret + Pages enabled (build_type=workflow). Demo URL: https://linroger.github.io/china-hsr-simulation/ (set as repo homepageUrl + in the description).
  - **Media:** README videos now render inline via `<video>` tags pointing to GitHub release `demo-media` assets (H.264 clips), with image stills; broken markdown video links removed.
  - **Docs:** regenerated `oceanbase-yearly-summary.json` from the live 1,800-route data (8.25M trains / 3.87B pax / ¥1.49T / 657k route-day rows / 3,600 variants) so the dashboard is accurate; README/README.zh-CN doc-drift corrected (tests 32, snapshot 200 ms/5 Hz, train cap 800, 1,800 routes, LoC, OSM caps).
  - **Tests:** 30 → **32/32** (added cancellation-accounting + delay-cascade tests; extended surge test with expiry). `npm run perf` now has coarse regression gates.
